[project]
name = "alfaeq-scrapling-worker"
version = "1.0.0"
description = "Server-side Scrapling ingestion worker for Alfaeq Yemen"
requires-python = ">=3.10"
dependencies = [
  "scrapling[fetchers]==0.4.15",
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.34,<1",
]

[tool.uv]
package = false
